include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
char *ChargerChaine(int){int n;
     char *ch = malloc(n*sizeof(int));
     printf("donner la taille de la chain : ",n);
     scanf("%d",&n);
     gets(ch);
     return ch;}
int longueur(char*ch){
    int m = longueur(ch);
    scanf("%c",&ch);
    for(p = m;p < m;p++)
    c=0;
    if(ch(p)!="/0")
    c++;
    printf("c = %d",c);
    return c;
